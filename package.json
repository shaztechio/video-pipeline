{
  "name": "video-pipeline",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/editor",
    "dev": "npm run dev -w packages/editor",
    "prepare": "npm run build",
    "test": "npm test -w packages/cli"
  }
}
